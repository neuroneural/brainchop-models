[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "bcmodel"
version = "0.1.0"
description = "Python bindings for .bcmodel brain MRI segmentation model parser"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
