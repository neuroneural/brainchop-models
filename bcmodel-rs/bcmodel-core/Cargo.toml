[package]
name = "bcmodel-core"
version = "0.1.0"
edition = "2021"
description = "Core parser for .bcmodel brain MRI segmentation model files"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
